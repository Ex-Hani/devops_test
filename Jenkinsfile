pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                echo 'Building the application...'
                // Шаги для сборки приложения (например, компиляция, упаковка)
            }
        }
        stage('Test') {
            steps {
                echo 'Running tests...'
                // Шаги для запуска тестов приложения
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying the application...'
                // Шаги для развертывания приложения (например, использование Docker и Ansible)
            }
        }
    }
}
